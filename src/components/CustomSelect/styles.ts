import { COLOR } from "../../ui";

export const CustomStyles = {
  control: () => ({
    width: "350px",
    height: "50px",
    background: COLOR.WHITE,
    borderRadius: "4px",
    cursor: "pointer",
  }),

  indicatorSeparator: () => ({
    width: 0,
  }),
};
