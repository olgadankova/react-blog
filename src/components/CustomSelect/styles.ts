import { COLOR } from "../../ui";

export const CustomStyles = {
  control: () => ({
    width: "250px",
    height: "50px",
    background: COLOR.WHITE,
    borderRadius: "4px",
  }),

  indicatorSeparator: () => ({
    width: 0,
  }),
};
